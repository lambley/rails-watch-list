<%= simple_form_for [@bookmark] do |f| %>
  <%= f.input :comment, :label => "Bookmark comment" %>
  <%= f.association :list, selected: (@list ? @list.id : 1) %>
  <%= f.association :movie %>
  <%= f.submit "Create bookmark" %>
<% end %>
